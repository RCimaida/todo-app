<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>TODOアプリ</title>
    <style>
        body { font-family: sans-serif; max-width: 500px; margin: 40px auto; }
        .done { text-decoration: line-through; color: gray; }
        .deadline { color: red; font-size: 0.9em; margin-left: 10px; }
    </style>
</head>
<body>
    <h1>TODOアプリ</h1>

    <form action="/add" method="POST">
        <input type="text" name="title" placeholder="TODOを入力">
        <input type="date" name="deadline">
        <button type="submit">追加</button>
    </form>

    <ul>
        {% for todo in todos %}
        <li>
            <span class="{{ 'done' if todo.done else '' }}">
                {{ todo.title }}
            </span>
            {% if todo.deadline %}
            <span class="deadline">📅 {{ todo.deadline }}</span>
            {% endif %}
            <a href="/complete/{{ todo.id }}">✅完了</a>
            <a href="/delete/{{ todo.id }}">🗑️削除</a>
        </li>
        {% endfor %}
    </ul>
</body>
</html>